# stockCrawler
~~~

# docker build:
goto docker dir
sudo docker build -t="stockcrawler" .
# docker run:
sudo docker run -t -i stockcrawler /bin/bash
sudo docker run -i -t -d stockcrawler /bin/bash ./home/ubuntu/stockCrawler/crawlnetease.sh